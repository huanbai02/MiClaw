# AGENTS.md

MiClaw 是一个本地优先、可观察、可约束、可扩展的终端 AI Agent。后续开发应优先保证安全边界、稳定性和可维护性，而不是堆叠功能。

## 开发前检查

- 先阅读相关代码和测试，不要凭记忆改动接口、路径或依赖。
- 修改前查看 `git status --short`，避免覆盖用户未提交改动。
- 优先使用现有结构：`miclaw/core/` 放核心逻辑，`entry/` 放 CLI/交互入口，`tests/` 放回归测试，`docs/` 放用户文档；复杂能力增长时再按“模块边界与演进方向”逐步拆分。
- 不读取、打印或提交 `.env`、密钥、用户隐私文件。
- 不把运行产物、缓存、日志、workspace 内容纳入核心代码，除非任务明确要求。

## 模块边界与演进方向

* 当前优先保持现有目录结构，不为单个小改动主动重排目录。
* 当 `miclaw/core/` 中某类能力开始变复杂时，应逐步收敛为清晰子模块，而不是继续堆在单个文件中。
* 推荐演进边界：

  * `agent/`：Agent loop、LangGraph state、节点和上下文组装。
  * `tools/`：内置工具、工具 schema、权限、执行结果和错误格式。
  * `skills/`：SKILL.md 加载、校验、缓存、启停和统计。
  * `memory/`：用户画像、项目记忆、任务记忆和记忆注入记录。
  * `scheduler/`：任务定义、心跳、状态、重试和运行历史。
  * `observability/`：JSONL 日志、trace、monitor 数据模型和调试信息。
  * `mcp/`：MCP server 配置、client、tool adapter 和调用记录。
  * `sandbox/`：office 路径安全、shell 执行后端和未来可选容器沙盒。
* 拆分必须渐进进行：先抽离稳定的数据结构或纯函数，再迁移调用方，最后补测试和文档。
* 不允许为了“架构更好看”进行一次性大规模搬迁、重命名或循环依赖式重构。


## 编码规则

- 变更保持最小、聚焦，避免无关重构、重命名和大规模格式化。
- 代码应清晰直接，优先显式逻辑，避免过度抽象。
- 新增核心逻辑必须补测试；修 bug 先补能复现问题的回归测试。
- 公共函数、复杂内部函数需要简洁 docstring：说明用途、参数、返回值；不要写模板化废话或重复代码本身。
- 错误处理要明确，不要静默吞异常；用户可见错误应给出可行动信息。
- 不新增依赖，除非确有必要；新增依赖时同步更新 `requirements.txt` 和安装/使用文档。
- 文件、shell、模型、网络等外部输入都视为不可信，必须做边界检查。
- 安全相关代码优先使用标准库可靠机制，不依赖字符串前缀或脆弱正则作为唯一保护。

## Sandbox 与工具安全

- `office/` 是模型文件读写和 shell 执行的唯一工作区。
- 路径校验应基于真实路径包含关系，例如 `Path.resolve()` + `relative_to()` 或 `os.path.commonpath()`。
- 必须拦截 `..`、绝对路径、Windows 盘符路径、符号链接逃逸等越权访问。
- shell 执行默认最小权限、有限超时、保留 stdout/stderr/exit code；高风险命令应有明确阻断或审批机制。
- 新增工具时要定义清晰参数 schema、权限边界、失败返回和测试用例。

## 测试与验证

常用验证命令：

```bash
python -m pytest
miclaw --help
miclaw config --help
miclaw monitor --help
```

- 改核心逻辑后运行相关单测；影响面不确定时运行完整 `python -m pytest`。
- 改 CLI 后至少验证对应 `--help` 能正常启动。
- 改配置、依赖或入口命令后同步验证 editable install 或入口导入。
- 如果测试无法运行，必须说明原因和已做的替代验证。

## 文档同步规则

- 用户可见能力、命令、配置项、目录结构变化时，同步更新 `README.md`。
- 复杂功能或设计约束写入 `docs/`，不要把 README 写成实现细节手册。
- 新增环境变量时同步 `.env.example`、README 配置说明和相关测试。
- 新增/调整技能机制时同步 `docs/` 中的技能使用说明。
- 文档用词保持准确，不夸大能力；未实现的能力不要写成已支持。

## 项目开发方向大纲

优先级从高到低：

1. 安全底座：修复 sandbox 路径校验、shell 权限控制、危险操作审计。
2. 工具体系：统一工具 schema、权限等级、超时、错误格式和测试覆盖。
3. Skill 生态：完善 SKILL.md 元数据、启停/安装/校验、懒加载和多文件技能支持。
4. MCP 接入：支持配置 MCP server，将外部 tools 安全接入 Agent。
5. Memory/Context：区分用户画像、项目记忆、任务记忆和短期摘要，保证可审计注入。
6. Observability：完善 JSONL trace、monitor、日志路径配置、token/latency/error 记录。
7. Scheduler：把 `tasks.json` 轮询逐步升级为可持久化、可暂停、可重试的任务系统。
8. 工程化：补齐 `pyproject.toml`、ruff/mypy/CI、更多边界测试和架构文档。

## 交付要求

- 交付前说明改了哪些文件、为什么改、如何验证、还存在哪些风险。
- 不主动 commit、push 或重写历史，除非用户明确要求。
- 遇到安全边界、数据丢失、依赖大改或架构取舍时，先说明影响再继续。
